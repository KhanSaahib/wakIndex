# Description: Synthetic constrained Codex workspace configuration with explicit extra access.
sandbox_mode = "workspace-write"

[sandbox_workspace_write]
network_access = true
writable_roots = ["../shared-infrastructure"]
